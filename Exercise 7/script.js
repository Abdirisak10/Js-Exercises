const colors=["White", "Black", "Green"];


// Adding color at the end
console.log("Adding color at the end")
colors.pop=("Blue")
console.log(colors);

// Removing the first color
console.log("Removing the first color")
colors.shift()
console.log(colors)
